diff a/src/components/Question.vue b/src/components/Question.vue	(rejected hunks)
@@ -34 +34 @@
-import Vue, { PropType } from 'vue';
+import Vue, { PropType } from "vue";
@@ -36 +36 @@ import Vue, { PropType } from 'vue';
-import { Perspective, RequestStatus, Status } from '@/types';
+import { Perspective, RequestStatus, Status } from "@/types";
@@ -45,2 +45,2 @@ export default Vue.extend({
-    clickedOption: '',
-    options: [1, 2, 3, 4, 5, 6, 7],
+    clickedOption: "",
+    options: [1, 2, 3, 4, 5, 6, 7]
@@ -51 +51 @@ export default Vue.extend({
-      required: true,
+      required: true
@@ -55 +55 @@ export default Vue.extend({
-      default: false,
+      default: false
@@ -59,2 +59,2 @@ export default Vue.extend({
-      required: true,
-    },
+      required: true
+    }
@@ -73 +73 @@ export default Vue.extend({
-      this.$emit('user-decided', {
+      this.$emit("user-decided", {
@@ -75 +75 @@ export default Vue.extend({
-        questionId: this.question.id,
+        questionId: this.question.id
@@ -77,2 +77,2 @@ export default Vue.extend({
-    },
-  },
+    }
+  }
@@ -130 +130 @@ export default Vue.extend({
-  content: '';
+  content: "";
